=content_for :header do
  = render :partial => "layouts/header"

.form
  %h2 Editing event

  = render 'form'

  .shared-links
    = link_to 'Show', agent_event_path(@agent, @event)
    \|
    = link_to 'Back', agent_events_path
