=content_for :header do
  = render :partial => "layouts/event_detail_header"

.event-detail
  #notice= notice
  .type.clearfix
    .title
      .event-type
        = @event.event_type
      .happen_time
        = @event.happen_time
    .mood
      = image_tag("#{@event.mode}.png")
  .description
    = @event.description

.action
  = button_to "Delete", agent_event_path(@agent, @event), :method => :delete, data: {confirm: 'Are you sure?'}